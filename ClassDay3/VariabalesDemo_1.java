package ClassDay3;

public class VariabalesDemo_1 {
    public static void main(String[] args) {
        int age=10;//requsting the computer to create abox type int and store a value 10 in it.
        int salary = 0; // we can only create a box of type int and don't assign any value it.
        salary=120000; // we can update a variable and assign a value to it later.
        System.out.println(salary);
       String name,city,country;// we can also make mulitple strings or any veriables  at the same time.
       char gender,letter;
       // we will get an error if we print it becasue there is no value to it.


    }
}
